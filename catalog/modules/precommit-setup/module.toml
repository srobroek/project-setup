schema_version = "1.0"

[meta]
repository = "github.com/your-org/your-repo"
author = "Sjors Robroek"

[module]
id = "precommit-setup"
name = "Pre-commit configuration"
version = "1.1.0"  # x-release-please-version
description = "Scaffold .pre-commit-config.yaml with commit-stage hygiene + pre-push secret scan, and vendor the close-keywords commit-msg scripts into .pre-commit-hooks/."
default_enabled = false
reconcile = true

[order]
after = ["dirs-scaffold"]

[[steps]]
id = "write"
kind = "python"
